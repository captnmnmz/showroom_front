<!DOCTYPE html>
<html lang="fr">
  <h1>Update deal</h1>

  <form action="/deals/update/{{deal.id}}" method="post">
    <div class="form-group">
      <label for="dealname">Name of the deal*</label>
      <input value="{{deal.name}}" class="form-control" type="text" id="dealname" name="dealname" placeholder="Name" required>
    </div>

    <div class="form-group">
      <label for="description">Description of the deal</label><br/>
      <textarea value ="{{deal.description}}" name="description" id="description" rows="5" cols="40"></textarea>
    </div>

      <label for="price">Price</label>
      <input value="{{deal.price}}" class="form-control" type="number" id="price" name="price" placeholder="Price of the deal">
    </div>

    <div class="form-group">
      <label for="begin">Begin date*</label>
      <input value="{{deal.begin}}" class="form-control" type="date" id="begin" name="begin" placeholder="Begin date" required>
    </div>

    <div class="form-group">
      <label for="end">End date*</label>
      <input value="{{deal.end}}" class="form-control" type="date" id="end" name="end" placeholder="End date" required>
    </div>

    <div class="form-group">
      <p>
      Do you want to hide this deal ? If not, it will be displayed on the map.  
      <!-- Way to conserve Y/N answer-->
      <input class="form-control" type="radio" id="yes" name="hidden"/><label for="yes">Yes</label>
      <input class="form-control" type="radio" id="no" name="hidden"/><label for="no">No</label>
      </p>
    </div>

    <div class="text-center">
      <button type="submit" class="btn btn-primary">
        Update
      </button>
    </div>
  </form>
</html>